#macro( isOdd $number $oddText $evenText ) #if( 0 == $number%2)$evenText#else $oddText#end#end
#parse( "head.vm")
  <body>
#parse ("top.vm")

<p>
Results $pager.currentPage.startOffsetLabel  - $pager.currentPage.endOffsetLabel 
	of $result.totalCount from the #if ($source) $source.title collection of the #end <a href="http://mokk.bme.hu/eszkozok/hunglishkorpusz">Hunglish corpus</a>

</p>


<table class="senttable">
#set ( $index = $request.startOffset + 1 )

#foreach( $hit in $result.hitList )
#if ( $row_class == "even" )
      #set ( $row_class = "odd" )
 #else
      #set ( $row_class = "even" )
#end
<tr class="$row_class">
 
<td class="notlast">
$hit.source.title
</td>

<!-- $hit.senId -->
<td class="notlast">


$hit.leftSentence
</td>
<td>
$hit.rightSentence
</td>
	</tr>
</div>
#end
</table>
<div class="pager">
#if ( $pager.prev || $pager.next)
#if ( $pager.prev )
        <a href="$pager.prev.url"> &lt;&lt;</a>
#end
#foreach ($page in $pager.pages)
#if ($page.current == true)
        $page.label
#else
        <a href="$page.url">$page.label</a>
#end
#end
#if ( $pager.next )
                <a href="$pager.next.url"> &gt;&gt;</a>
#end
#end

</div>
</body>


